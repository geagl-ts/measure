import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    titulo: {
        fontSize: 23,
        fontWeight: "bold",
        color: "#2a2a2a",
        textTransform: "capitalize",
        letterSpacing: 1,
    },
    picker: {
        width: "90%",
        height: 50,
        color: "#2a2a2a",
    },
});
