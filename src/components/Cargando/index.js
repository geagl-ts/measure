import React from "react";
import { ActivityIndicator } from "react-native";

export default () => {
    return (
        <ActivityIndicator
            color="#2ba6ff"
            size="small"
            style={{ margin: 10 }}
        />
    );
};
