import { StyleSheet } from "react-native";

import { BLACK_COLOR } from "./localConstants";

const styles = StyleSheet.create({
    title: {
        fontSize: 28,
        fontWeight: "bold",
        color: BLACK_COLOR,
        textTransform: "lowercase",
    },
});

export default styles;
